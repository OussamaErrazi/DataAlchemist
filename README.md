# 🧪 DataAlchemist

**DataAlchemist** is a lightweight, modular data transformation pipeline built with **Spring Boot**, **Kafka**, **Zookeeper**, and **Docker**.  
The goal is simple: take raw data (JSON/CSV), clean it, transform it, and export it — all through a Kafka-powered pipeline.